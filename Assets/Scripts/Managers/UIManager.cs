using UnityEngine;

namespace ProjectI
{
    /// <summary>
    /// HUD · 메뉴 · 화면 흐름. (기획서 PART 11)
    /// 미니멀 HUD(체력·스태미너·밝기·시간·무게·핫바·퀘스트).
    /// TODO: HUD 요소, 밝기 표시, 정산 화면. (Phase 1 일차 6~, Phase 3 블록 F)
    /// </summary>
    public class UIManager : MonoBehaviour
    {
        private void Awake()
        {
            Debug.Log("[UIManager] 초기화 (스텁)");
        }
    }
}
